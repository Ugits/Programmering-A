package com.jonas.demo;

/*TODO
*   Go back to the way the project was with public + PSVM
    Create TWO variables: number & Number (note the capital N) Print both - what happens?*/
public class PartSix {

    public static void main(String[] args) {

        int number = 1;
        int Number = 2;
        System.out.println(number); // prints 1
        System.out.println(Number); // prints 2

        /*  - The standard for variable names is to start with a lowercase letter
         *   - Both options can technically be used.
         *   - While 'number' is the preferred choice for a variable name,
         *    'Numbers' are actually, an abstract data type, aka a class.   */
    }
}
